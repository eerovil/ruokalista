# ruokalista

A recipe store.

Nothing is built yet, and the stack is deliberately undecided. Planning for this
project runs through the `/wayfinder` skill: a map issue on GitHub Issues holds
the destination, and its child tickets hold the open decisions, resolved one at
a time.

See `docs/agents/issue-tracker.md` for how the tracker is wired up.
