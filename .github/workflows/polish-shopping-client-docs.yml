name: Polish shopping client docs

on:
  push:
    branches:
      - docs/279-shopping-client-cleanup

permissions:
  contents: write

jobs:
  polish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/polish-shopping-client-docs.mjs
      - name: Commit documentation polish
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/codebase/screens.md .github/workflows/polish-shopping-client-docs.yml scripts/polish-shopping-client-docs.mjs
          git diff --cached --quiet && exit 0
          git commit -m "Polish shopping client documentation"
          git push
