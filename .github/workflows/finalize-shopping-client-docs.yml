name: Finalize shopping client docs

on:
  push:
    branches:
      - docs/279-shopping-client-cleanup

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/finalize-shopping-client-docs.mjs
      - name: Commit final documentation cleanup
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/codebase/screens.md src/shopping-screens.ts .github/workflows/finalize-shopping-client-docs.yml scripts/finalize-shopping-client-docs.mjs
          git diff --cached --quiet && exit 0
          git commit -m "Finalize shopping client documentation"
          git push
