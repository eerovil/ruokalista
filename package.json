{
  "name": "ruokalista",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply ruokalista --local",
    "db:seed:local": "wrangler d1 execute ruokalista --local --file=dev/seed.sql",
    "db:smoke:local": "wrangler d1 execute ruokalista --local --command=\"SELECT COUNT(*) AS tables FROM sqlite_master WHERE type = 'table' AND name IN ('household','member','recipe','meal_entry');\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.31.0"
  }
}
